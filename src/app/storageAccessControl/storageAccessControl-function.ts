// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for StorageAccessControlFunction
 */
export interface StorageAccessControlFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/storageAccessControl/storageAccessControl.
 */
export class StorageAccessControlFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: StorageAccessControlFunctionProps) {
    super(scope, id, {
      description: 'src/app/storageAccessControl/storageAccessControl.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/storageAccessControl/storageAccessControl.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}